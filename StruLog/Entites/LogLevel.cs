namespace StruLog.Entites
{
    public enum LogLevel
    {
        TRACE = 100,
        DEBUG = 200,
        INFO = 300,
        IMPORTANT = 400,
        WARNING = 500,
        ERROR = 600,
        FATAL = 700

    }
}
